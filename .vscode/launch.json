
{
	"version": "0.0.4",
    "configurations": [
        {
            "name": "Extension",
            "type": "extensionHost",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": [
                "${workspaceFolder}/test_files/test_script.mxl",
                "--extensionDevelopmentPath=${workspaceFolder}",

            ]
        }
    ]
}